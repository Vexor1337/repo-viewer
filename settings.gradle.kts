
rootProject.name = "repo-viewer"
include(
    "monolith:app:application",
    "monolith:app:infrastructure",
    "monolith:app:interfaces",
    "monolith:app:ports-input",
    "monolith:app:ports-output",
    "monolith:config",
    "monolith:app:commons",

)